---
description: 用户偏好和开发原则规则
alwaysApply: false
---
# 用户偏好和开发原则规则

## 🚫 严格禁止的行为

### 1. UI风格和功能保护
- **禁止随意修改UI风格**: 未经用户明确允许，不得更改现有的UI设计、布局、颜色方案、动画效果
- **禁止删除或简化功能**: 不得为了"修复"问题而移除或简化已有功能
- **禁止破坏性重构**: 不得在没有用户同意的情况下进行大幅度的代码重构
- **保持设计一致性**: 必须维护项目现有的设计语言和用户体验

### 2. 功能完整性保证
- **功能不得缺失**: 修复bug时必须保证所有原有功能正常工作
- **不能用减法解决问题**: 禁止通过删除代码来"解决"问题
- **保持向后兼容**: 确保修改后的代码与现有系统完全兼容
- **功能增强优先**: 优先通过增强现有功能而非替换来解决问题

## 🔍 问题排查原则

### 1. 当用户反馈"修改没效果"时的排查步骤

#### 第一步：验证修改路径
```dart
// 使用明显的测试值验证代码路径是否正确
// 例如：将间距设置为极大值(50px)来确认修改生效
const SizedBox(height: 50), // 测试值，确认代码路径
```

#### 第二步：检查目标组件
- **确认组件选择**: 验证修改的是否为实际渲染的组件
- **检查条件分支**: 确认代码是否在正确的if/else分支中执行
- **平台差异检查**: 验证iOS/Android平台是否使用不同的组件

#### 第三步：深入分析根本原因
- **系统级约束**: 检查是否存在平台系统级的默认行为
- **CSS继承问题**: 分析样式是否被其他规则覆盖
- **布局算法**: 理解Flutter布局系统的实际行为
- **缓存问题**: 检查是否存在缓存导致的问题

#### 第四步：渐进式问题定位
```dart
// 示例：逐步缩小问题范围
// 1. 先用极端值测试
Container(
  height: 200, // 极端测试值
  color: Colors.red, // 可视化调试
  child: YourWidget(),
)

// 2. 逐步调整到目标值
Container(
  height: 50, // 接近目标值
  child: YourWidget(),
)

// 3. 应用最终值
Container(
  height: 20, // 最终目标值
  child: YourWidget(),
)
```

### 2. 系统性调试方法

#### 代码路径验证
- **添加调试输出**: 使用`debugPrint()`确认代码执行
- **可视化调试**: 使用明显的颜色和尺寸变化
- **断点调试**: 在关键位置设置断点验证执行流程

#### 组件层次分析
- **Widget树检查**: 使用Flutter Inspector分析组件结构
- **布局约束理解**: 深入理解父子组件的约束关系
- **渲染流程跟踪**: 分析从数据到UI的完整渲染流程

## 📋 修改前必须检查的清单

### 1. 功能保护检查
- [ ] 确认所有现有功能在修改后仍然正常工作
- [ ] 验证UI交互逻辑没有被破坏
- [ ] 检查数据流和状态管理没有问题
- [ ] 确认用户体验没有降级

### 2. 风格一致性检查
- [ ] 新增代码符合项目现有的设计规范
- [ ] 颜色、字体、间距与现有风格一致
- [ ] 动画效果与整体应用风格协调
- [ ] 组件行为符合用户期望

### 3. 兼容性检查
- [ ] iOS和Android平台表现一致
- [ ] 不同屏幕尺寸适配正常
- [ ] 不影响其他页面或组件的功能
- [ ] 向后兼容性得到保证

## 🎯 正确的问题解决方式

### 1. 分析优于修改
- **先理解再行动**: 深入分析问题根因，而非急于修改代码
- **最小化修改**: 用最小的代码改动解决最大的问题
- **保守式优化**: 采用保守的修改策略，避免引入新问题

### 2. 增量式解决方案
- **渐进式改进**: 小步骤、多验证的改进方式
- **功能增强**: 通过增强现有功能解决问题，而非替换
- **向上兼容**: 确保新方案完全兼容现有系统

### 3. 用户反馈驱动
- **及时响应**: 当用户反馈问题时，立即深入调查
- **详细说明**: 向用户解释发现的根本原因和解决方案
- **确认效果**: 修改后确认用户满意度

## 🚨 紧急情况处理

### 当遇到"顽固"问题时
1. **停止盲目尝试**: 不要反复修改相同的代码
2. **系统性分析**: 从架构层面分析问题
3. **寻找替代方案**: 探索其他技术手段解决问题
4. **记录发现**: 将调试过程和发现记录下来

### 平台特异性问题
```dart
// 正确的平台判断和处理方式
import 'dart:io';

Widget buildPlatformSpecificWidget() {
  if (Platform.isIOS) {
    // iOS特有的解决方案
    return Transform.translate(
      offset: const Offset(0, -20),
      child: YourWidget(),
    );
  } else {
    // Android正常处理
    return YourWidget();
  }
}
```

## 💡 最佳实践总结

### 开发心态
- **保守且谨慎**: 宁可多花时间分析，也不要贸然修改
- **尊重现有设计**: 理解并尊重项目既有的架构和设计决策
- **用户体验优先**: 始终以用户体验和功能完整性为最高优先级

### 沟通方式
- **详细解释**: 向用户详细说明发现的问题和解决思路
- **征求意见**: 重大修改前征求用户同意
- **及时反馈**: 遇到复杂问题时及时向用户反馈调查进展

---

**核心原则**: 理解问题胜过修复问题，保护功能胜过快速解决，用户满意胜过技术完美。
**注意事项**: 当功能实现，且用户实测后反馈成功的时候，还有用户说好的时候，要更新CLAUDE.md。