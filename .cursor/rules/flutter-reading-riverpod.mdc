---
description: reader_page.dart 开发准则
globs: lib/pages/reader_page.dart
---
# Reader Page 构建约束

- 该文件必须使用 Riverpod 进行状态管理；如需 `AnimationController` 可使用 `ConsumerStatefulWidget` 作为壳组件，但禁止在其中直接调用 `setState` 更改业务状态，业务状态必须集中在 `StateNotifier`/`Notifier` 中维护。
- 阅读分页逻辑需要复用 `lib/services/advanced_text_paginator.dart` 与 `lib/utils/text_painter_pagination.dart` 的模式，分页在后台 isolate 完成并通过缓存加速。
- UI 分层：根组件 `ReaderPage` 负责组合子组件；`ReaderOverlay`、`ReaderToolbar`、`ReaderTextView` 等复杂组件独立成私有类，每个 build 方法限定 80 行以内。
- 工具栏的动画使用 `AnimationController` + `FadeTransition`/`SlideTransition`，禁止使用 `AnimatedOpacity` 等隐式动画造成抖动。
- 每种翻页模式封装为独立 widget：`_SlidePaginationView`、`_ScrollPaginationView`、`_SimulationPaginationView`，通过 provider 状态切换并保持页面索引同步。
- TTS 逻辑依赖现有 `lib/services/tts/system_tts.dart` 能力；朗读时根据当前页内句子高亮显示，需暴露 provider 状态给渲染层使用。
- 构建完成后的校验流程固定为：`flutter analyze` → `flutter test` → `flutter build apk --debug`，发现错误必须修复后再继续开发。
