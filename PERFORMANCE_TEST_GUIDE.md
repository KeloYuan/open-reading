# 性能优化测试指南

## 测试环境准备

1. **数据库升级测试**
   - 运行应用,确保数据库自动升级到版本9
   - 检查日志中是否有数据库升级信息

2. **准备测试数据**
   - 导入几本有封面的书籍
   - 确保至少有一些书籍有 `coverImagePath` 字段

---

## 功能测试清单

### 1. 数据库性能测试 ✓

**测试步骤:**
1. 打开应用
2. 导入一本书籍(测试重复检测速度)
3. 使用搜索功能搜索书名或作者
4. 查看书籍列表排序

**预期结果:**
- 重复检测应该很快(<100ms)
- 搜索结果应该立即显示
- 列表排序流畅无卡顿

---

### 2. 统计页面封面显示测试 ✓

**测试步骤:**
1. 打开应用主页
2. 点击进入"阅读统计详情"
3. 查看"总览"标签页的"最近阅读"部分
4. 查看"书籍"标签页的"阅读时长排行"

**预期结果:**
- 有封面的书籍应显示真实封面图片
- 没有封面的书籍显示默认图书图标
- 封面图片应正确适配40x56尺寸
- 界面美观,无错位

**截图位置:**
- 总览 → 最近阅读
- 书籍 → 阅读时长排行

---

### 3. WebDAV同步测试 ✓

**准备工作:**
1. 准备一个WebDAV服务器(如坚果云、NextCloud等)
2. 获取服务器地址、用户名、密码

**测试步骤:**

#### 3.1 配置测试
1. 打开应用设置
2. 找到WebDAV同步设置
3. 输入服务器信息:
   - 服务器地址: `https://your-server.com/dav/`
   - 用户名: `your-username`
   - 密码: `your-password`
4. 点击"测试连接"
5. 保存配置

**预期结果:**
- 连接测试成功
- 显示"连接成功"提示

#### 3.2 元数据同步测试
1. 点击"立即同步"按钮
2. 观察同步状态变化
3. 检查WebDAV服务器目录结构

**预期结果:**
- 同步状态: 空闲 → 同步中 → 同步完成 → 空闲
- WebDAV服务器上应创建以下目录:
  ```
  xxread/
  ├── books/books.json        ← 书籍元数据
  ├── bookmarks/bookmarks.json
  ├── progress/progress.json
  ├── notes/
  └── files/                  ← 书籍文件目录
  ```

#### 3.3 书籍文件同步测试
1. 导入几本书籍(建议小于100MB)
2. 执行同步
3. 查看日志输出
4. 检查WebDAV的 `xxread/files/` 目录

**预期结果:**
- 日志显示上传进度:
  ```
  已上传书籍: 《书名》 (1234.5KB)
  书籍文件上传完成: 上传 3 个, 跳过 0 个
  ```
- `xxread/files/` 目录下应该有对应的书籍文件
- 文件名格式: `{content_hash}.{format}`

#### 3.4 增量同步测试
1. 再次点击"立即同步"
2. 观察日志输出

**预期结果:**
- 已存在的文件应被跳过:
  ```
  文件已存在,跳过: 《书名》
  书籍文件上传完成: 上传 0 个, 跳过 3 个
  ```

#### 3.5 大文件跳过测试
1. 导入一本>100MB的大书籍
2. 执行同步
3. 查看日志

**预期结果:**
- 日志显示:
  ```
  跳过过大文件 (123.4MB): 《大书籍》
  ```

---

## 性能对比测试

### 数据库查询性能

**测试方法:**
1. 使用开发者工具或添加日志计时
2. 测试以下操作的响应时间:
   - 导入书籍时的重复检测
   - 书名搜索
   - 作者搜索
   - 按导入时间排序

**预期改善:**
| 操作 | 优化前 | 优化后 | 提升 |
|------|--------|--------|------|
| 重复检测 | ~100ms | ~5ms | 20倍 |
| 书名搜索 | ~200ms | ~10ms | 20倍 |
| 日期排序 | ~150ms | ~15ms | 10倍 |

### WebDAV同步性能

**测试方法:**
1. 计时整个同步过程
2. 记录上传的数据量

**测试场景:** 100本书的元数据同步

**预期改善:**
| 指标 | 优化前 | 优化后 | 提升 |
|------|--------|--------|------|
| 数据量 | ~10MB | ~3MB | 减少70% |
| 同步时间 | ~60s | ~20s | 提升3倍 |

---

## 常见问题排查

### 封面不显示
1. 检查 `coverImagePath` 字段是否有值
2. 检查文件路径是否存在
3. 查看日志中的图片加载错误

### WebDAV同步失败
1. 检查网络连接
2. 验证服务器地址、用户名、密码
3. 查看服务器端错误日志
4. 确认服务器支持PROPFIND、MKCOL、PUT等WebDAV方法

### 书籍文件未上传
1. 检查文件是否存在
2. 检查文件大小(<100MB)
3. 查看日志输出的跳过原因
4. 验证 `content_hash` 字段是否存在

---

## 回归测试

确保以下功能未受影响:

- [ ] 书籍导入功能正常
- [ ] 阅读功能正常
- [ ] 翻页功能正常
- [ ] 书签功能正常
- [ ] 笔记高亮功能正常
- [ ] 统计数据记录正常
- [ ] 搜索功能正常
- [ ] 设置保存正常

---

## 注意事项

1. **数据备份**: 测试前建议备份数据库和书籍文件
2. **网络环境**: WebDAV测试需要稳定的网络连接
3. **日志收集**: 出现问题时请保存完整日志
4. **版本兼容**: 确保使用的Flutter版本符合要求
