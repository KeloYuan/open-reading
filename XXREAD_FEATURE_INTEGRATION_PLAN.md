# xxread 功能整合与开发计划

本文档详细规划了 `xxread` 应用的功能增强计划，旨在融合 `anx-reader` 和 `legado` 的优点，同时保持应用现有的UI风格和用户体验。

开发过程将遵循以下待办事项清单。每当一个功能开发完成并通过测试后，对应的项目将被勾选。

---

## Phase 1: 核心阅读引擎集成 (Core Reading Engine Integration)

**目标:** 集成基于WebView和原生Flutter的双阅读引擎，从根本上解决文本渲染问题，并为用户提供选择。这是所有后续阅读功能的基础。

- [ ] **1.1: 阅读引擎切换器 (Reading Engine Switcher)**
  - **描述:** 实现一个核心服务 (`ReadingEngineCoordinator`) 来管理和切换WebView阅读器和原生Flutter阅读器。
  - **要求:**
    - 在应用设置中提供一个选项，允许用户选择默认的阅读引擎。
    - 引擎切换逻辑应平滑，并能保存书籍的阅读进度。

- [ ] **1.2: WebView阅读页面 (WebView Reading Page)**
  - **描述:** 使用 `flutter_inappwebview` 创建一个专门的WebView阅读页面。
  - **要求:**
    - 利用 `anx-reader` 项目中的 `foliate-js` 库来渲染书籍内容。
    - 至少支持EPUB和TXT格式书籍的基本内容加载和显示。
    - 确保页面性能流畅，特别是在长篇文档上。

- [ ] **1.3: 原生阅读页面优化 (Native Reading Page Refinement)**
  - **描述:** 重构和优化现有的原生Flutter阅读页面 (`reading_page_enhanced.dart`)。
  - **要求:**
    - 彻底解决原生渲染时出现的缺字、漏字问题。
    - 如果问题过于复杂无法在短期内完美解决，此引擎将作为实验性选项，将WebView引擎设为主要稳定方案。

---

## Phase 2: 书源管理系统 (Book Source Management - Legado Style)

**目标:** 借鉴 `legado` 的设计，实现一个灵活、强大的书源管理系统，允许用户导入和管理多种来源以发现和阅读书籍。

- [ ] **2.1: 书源模型与存储 (Book Source Model & Storage)**
  - **描述:** 定义数据模型 (`BookSource`) 并创建相应的数据库表。
  - **要求:**
    - 使用 `isar` 或 `drift` 进行本地数据持久化。
    - 数据模型需包含源名称、URL、类型（例如，JSON API, RSS, HTML-Scrape）、认证信息以及关键的解析规则。

- [ ] **2.2: 书源导入功能 (Book Source Import)**
  - **描述:** 开发从外部导入书源的功能。
  - **要求:**
    - 支持从用户提供的文件（JSON/YAML格式）或URL导入。
    - 实现解析逻辑，将导入的源数据正确存入本地数据库。
    - 提供导入状态的反馈（成功、失败、格式错误）。

- [ ] **2.3: 书籍发现与搜索UI (Book Discovery/Search UI)**
  - **描述:** 构建一个用户界面，让用户可以从已配置的书源中浏览、搜索和导入书籍。
  - **要求:**
    - UI应清晰地展示所有可用的书源列表。
    - 根据不同书源的解析规则，正确地从远程获取数据并展示为书籍列表。
    - 实现搜索功能，允许用户在特定或所有书源中搜索书籍。

---

## Phase 3: 阅读器UI与功能对齐 (Reader UI & Feature Parity)

**目标:** 统一双引擎的阅读体验，确保笔记、划线、目录等核心功能在不同渲染模式下表现一致。

- [ ] **3.1: 统一的目录功能 (Unified Table of Contents - ToC)**
  - **描述:** 实现一个能够解析并展示书籍目录的服务。
  - **要求:**
    - 能够通过JavaScript交互从WebView中提取章节信息。
    - 能够解析原生引擎加载的书籍的目录结构。
    - 目录UI面板在两种模式下保持一致。

- [ ] **3.2: 翻页动画 (Page Turn Animation)**
  - **描述:** 为阅读器添加多种翻页动画效果。
  - **要求:**
    - 支持至少三种翻页方式：滑动、仿真翻页、无动画。
    - 动画效果在WebView和原生页面上均可流畅运行。WebView模式下可探索使用CSS或JS动画库实现。

- [ ] **3.3: 笔记与划线 (Notes and Highlighting)**
  - **描述:** 创建一个系统，允许用户在阅读时添加笔记和高亮划线。
  - **要求:**
    - WebView中通过JS捕获选中文本，并覆盖高亮层。
    - 笔记和划线数据与书籍的特定位置（如CFI）关联，并持久化存储在本地数据库。
    - 提供统一的UI来创建、查看和管理笔记。

- [ ] **3.4: 字体与主题定制 (Font and Theme Customization)**
  - **描述:** 允许用户高度自定义阅读界面。
  - **要求:**
    - 可调整项包括：字体大小、字体家族、行间距、段间距、页边距、背景色和文本颜色。
    - 所有设置必须同时对WebView和原生引擎生效。WebView模式下，通过动态注入CSS来实现样式更新。

---

## Phase 4: 数据同步与持久化 (Data Synchronization & Persistence)

**目标:** 使用WebDAV实现一个健壮的数据同步服务，用于备份和同步用户的核心数据。

- [ ] **4.1: WebDAV服务增强 (WebDAV Service Enhancement)**
  - **描述:** 重构和增强现有的 `webdav_sync_service.dart`。
  - **要求:**
    - 支持同步两种引擎的阅读进度、笔记、划线、书签以及书源列表。
    - 实现基础的冲突解决逻辑（例如，基于时间戳选择最新版本）。
    - 提高同步的稳定性和错误处理能力。

- [ ] **4.2: 自动与手动同步触发 (Automatic & Manual Sync Triggers)**
  - **描述:** 提供灵活的同步选项。
  - **要求:**
    - 在设置页面提供手动“立即同步”按钮。
    - 实现自动同步机制（例如，应用启动时、退出阅读页时）。
    - 在UI中清晰地展示同步状态（同步中、上次同步时间、失败信息）。

---

## Phase 5: UI/主题一致性与最终打磨 (UI/Theme Consistency & Final Polish)

**目标:** 确保所有新增功能和页面都严格遵循 `xxread` 现有的UI设计规范、主题和动画风格，提供无缝、统一的用户体验。

- [ ] **5.1: UI组件审查 (UI Component Audit)**
  - **描述:** 对所有新增的UI组件进行全面审查。
  - **要求:**
    - 确保新组件（如设置页、弹窗、面板）复用 `xxread` 已有的主题属性（颜色、字体、间距）和自定义Widget。
    - 避免引入与现有风格不符的新设计。

- [ ] **5.2: 动画整合 (Animation Integration)**
  - **描述:** 将 `xxread` 标志性的动画和页面过渡效果应用到新功能中。
  - **要求:**
    - 所有新页面的跳转需使用项目统一的过渡动画。
    - 按钮点击、列表项加载等交互应包含符合整体风格的微动画。
